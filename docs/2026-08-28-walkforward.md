# 워크포워드 검증 하니스 + 첫 결과 (2026-08-28)

## 왜 만들었나

이 프로젝트의 A/B 는 전부 **같은 데이터를 반복해서 보고** 그중 좋아 보이는 값을 채택해 왔다.
그렇게 채택한 값이 구간을 바꾸자 뒤집혔다 — 하루에 세 번 봤다(`blend` 랭킹, 눌림 3%, breadth 0.6).

in-sample 최적값은 "그 구간에서 뭐가 좋았나"를 말할 뿐, "다음 구간에서 뭐가 좋을까"를 말하지 않는다.

```
[학습 4년] → 규칙 선택 → [검증 1년, 한 번도 안 봄] → 성과 기록
     └ 1년 뒤로 밀어서 반복
```

## 하니스가 구조적으로 막는 것

오늘 실제로 겪은 결함들을 **어서션으로** 막는다. 안전장치가 죽어 있으면 없느니만 못하므로
(통과했다는 사실이 근거로 쓰이기 때문에) **일부러 깨뜨려 발동을 확인**했고 회귀테스트 20건으로 잠갔다.

| 겪은 결함 | 막는 장치 |
|---|---|
| `--start` 무시로 구간이 조용히 잘림 | `_assert_window` — 시뮬한 영업일 수를 요청 구간과 대조 |
| 학습/검증 누수 | `_assert_no_leak` — 검증구간 밖 진입이 하나라도 있으면 실패 |
| 폴드 경계 오류 | `_assert_folds` — 학습 종료 < 검증 시작 엄격 검사 |
| 워밍업 부족이 구간 축소로 둔갑 | 데이터는 전 구간 로드, **매매 판단만** `sim_from/sim_to` 로 제한 |
| 사후 선택("이게 좋네") | 후보 격자 `GRID` 와 선택 규칙 `select()` 를 **모듈 상수/함수로 사전 고정** |
| 생존편향 절대값 오독 | 결과를 **항상 동일 유니버스 동일가중 벤치마크와 함께** 출력 |

선택 규칙은 사람이 개입하지 않는다. 동점이면 `GRID` 선언 순서를 따르므로 재현 가능하고,
학습구간이 전부 손실이어도 규칙대로 '가장 덜 나쁜' 것을 고른다 — 실제 운용의 그 시점 판단이 그렇다.

## 첫 결과

```
학습 4년 / 검증 1년 롤링 · 선택기준 MAR · 후보 7개 · 폴드 8개
데이터 2015-01-02~2026-08-24 (2,853 영업일) · 유니버스 시총상위 100
```

| 검증 | 학습구간 | 선택된 규칙 | 학습MAR | 검증수익 | 검증MDD | 벤치마크 |
|---|---|---|---|---|---|---|
| 2019 | 2015-01~2018-12 | MA120 청산·슬롯5 | 0.06 | +8.8% | 14.1% | +2.0% |
| 2020 | 2016-01~2019-12 | MA120 청산·슬롯5 | 0.21 | +58.0% | 18.7% | +32.4% |
| 2021 | 2017-01~2020-12 | MA120 청산·슬롯5 | 0.53 | -1.5% | 23.8% | +20.1% |
| 2022 | 2018-01~2021-12 | MA120 청산·슬롯20 | 0.56 | -14.5% | 14.7% | -11.7% |
| 2023 | 2019-01~2022-12 | MA120 청산·슬롯20 | 0.24 | +22.8% | 16.0% | +35.5% |
| 2024 | 2020-01~2023-12 | MA200 청산·슬롯20 | 0.43 | -1.4% | 19.7% | +19.6% |
| 2025 | 2021-01~2024-12 | MA200 청산·슬롯20 | 0.04 | +51.8% | 14.6% | +83.1% |
| 2026 | 2022-01~2025-12 | MA120+시간120·슬롯20 | 0.82 | +74.3% | 22.3% | +35.2% |

```
OOS 누적(복리)   전략 +363.9%   벤치마크 +475.0%
플러스 연도      5/8
벤치마크 상회    3/8
연 평균          전략 +24.8%   벤치마크 +27.0%
```

### 읽는 법

**1. 현행 청산 사다리는 8개 폴드에서 한 번도 선택되지 않았다.**
후보에 넣어뒀는데(`현행 사다리·슬롯5`, `·슬롯20`) 학습구간 성적이 매번 뒤졌다.
2026-08-28 in-sample 진단(ATR 트레일이 범인)의 **독립적 확인**이다. 사람이 고른 게 아니라
기계적 선택 규칙이 매번 트레일 없는 쪽을 뽑았다.

**2. 그러나 세부 파라미터는 불안정하다.** 뽑힌 규칙이 폴드마다 4종류로 갈린다
(MA120·슬롯5 ×3 / MA120·슬롯20 ×2 / MA200·슬롯20 ×2 / MA120+시간120 ×1).
"MA120·슬롯20 이 최적" 같은 주장은 여전히 근거가 없다. 견고한 것은 **트레일 제거**뿐이다.

**3. 재개 조건 미충족.** OOS 누적이 벤치마크에 못 미친다(+363.9% vs +475.0%, 상회 3/8).

### 골대를 옮기지 않는다

전략의 폴드별 MDD 는 14~24% 로 낮고, 12년 벤치마크 MDD 는 51% 였다. 위험조정으로 보면
경쟁력이 있어 보인다 — **그러나 이것을 근거로 "통과"라고 하지 않는다.**
판정 기준은 결과를 보기 **전에** 선언했고(누적수익 벤치마크 초과), 결과를 본 뒤 기준을 바꾸는 것이
바로 이 프로젝트가 반복해 온 실패 방식이다.

위험조정 비교를 하려면 **다음 검증에서 그 기준을 미리 선언하고** 벤치마크의 폴드별 MDD 도
같은 방식으로 측정해야 한다. 그건 별도 작업이다.

## 결론

- 신규진입 **정지 유지**(`TREND_ENTRY_HALT=true`).
- 확정된 것 하나: **ATR 트레일은 제거 대상이다.** in-sample 진단과 워크포워드가 독립적으로 일치.
- 확정되지 않은 것: 어떤 청산으로 대체할지, 슬롯 몇 개인지. 폴드마다 답이 달라진다.
- 다음: 위험조정 기준을 **사전 선언**한 뒤 재검증. 그 전엔 어떤 파라미터도 라이브에 넣지 않는다.

## 사용법

```bash
python scripts/walkforward_trend.py                  # 기본(4년 학습/1년 검증, MAR 기준)
python scripts/walkforward_trend.py --train-years 3
python scripts/walkforward_trend.py --metric total
python -m pytest tests/test_walkforward.py -q        # 하니스 안전장치 회귀 20건
```


---

# 위험조정 기준 재검증 (2026-08-28, 2차)

## 기준 — 결과를 보기 전에 선언·커밋

사용자 결정 **"위험이 절반이면 채택"** 을 코드 상수로 고정하고, **측정 전에 커밋**했다
(`test(trend): 재개 판정 기준 사전 선언`). git 이력이 "사후에 기준을 고치지 않았다"는 증거다.

| # | 조건 | 이유 |
|---|---|---|
| ① | MDD(전략) ≤ **0.5** × MDD(벤치마크) | 선언된 '위험 절반' |
| ② | MAR(전략) ≥ MAR(벤치마크) | 위험을 줄인 대가로 수익을 더 잃지 않을 것 |
| ③ | 플러스 검증연도 ≥ 60% | 한두 해에 몰린 결과 배제 |

②가 없으면 **아무것도 안 사는 전략이 통과한다** — 위험 0, 수익 0. 그래서 하한을 함께 걸었다.

MDD·MAR 은 폴드별 지표의 평균이 아니라 **OOS 검증구간만 이어붙인 일별 자산곡선**에서 잰다.
폴드 경계를 넘는 연속 하락은 폴드별로 보면 두 개의 작은 낙폭으로 쪼개지지만, 운용자는 하나의 큰
낙폭을 겪는다(-20%/-20% 두 폴드 → 실제 -36%). 벤치마크도 같은 구간·같은 방식으로 잰다.

## 결과

```
OOS 이어붙인 자산곡선 (1,874 영업일, 7.4년)
                누적      CAGR      MDD     MAR
  전략        +363.9%   +22.9%   34.1%    0.67
  벤치마크     +475.0%   +26.5%   45.4%    0.58
```

| # | 조건 | 결과 | 판정 |
|---|---|---|---|
| ① | MDD 34.1% ≤ 45.4% × 0.5 = **22.7%** | 34.1% > 22.7% | **X** |
| ② | MAR 0.67 ≥ 0.58 | 우위 | O |
| ③ | 플러스 연도 5/8 = 62.5% ≥ 60% | 충족 | O |

**→ 조건 미충족. 신규진입 정지 유지.**

## 읽는 법

**위험은 줄었지만 절반은 아니다.** MDD 45.4% → 34.1% 은 벤치마크의 **75%** 다. 선언한 50% 에 못 미친다.

**다만 ②는 오늘 처음으로 통과한 조건이다.** MAR 0.67 vs 0.58 — 위험조정 기준으로는 벤치마크를
앞선다. 이것은 사전 선언된 기준으로, 한 번도 본 적 없는 구간에서, 기계적 선택 규칙으로 얻은
결과다. 오늘 나온 유일하게 신뢰할 만한 긍정 신호다.

그럼에도 **채택하지 않는다.** 이유 둘:
1. 사용자가 정한 기준은 ①이고, 결과를 본 뒤 기준을 완화하는 것이 이 프로젝트가 반복해 온 실패다.
2. 폴드마다 뽑힌 규칙이 4종류로 갈린다 — MAR 0.67 은 '어떤 특정 규칙'의 성적이 아니라
   '매년 학습구간 최적을 다시 고르는 절차'의 성적이다. 그 절차를 라이브로 옮기려면 매년
   재선택하는 운영 규칙까지 함께 정의해야 한다.

## '위험 절반'에 도달하려면

MDD 를 34.1% → 22.7% 로 낮추는 정직한 방법은 **노출 축소**다(슬롯당 비중↓, 현금 비중↑).
현재 격자는 전부 사실상 만기 투자(슬롯 5×20% / 20×5% = 100%)라 그 축이 아예 없다.

이건 기준을 바꾸는 게 아니라 **후보 공간을 넓히는 것**이라 정당하다. 단 결과를 본 뒤 격자를
넓히는 것 자체가 과적합 경로이므로, 넓힌 격자를 **먼저 선언·커밋하고** 그 결과를 그대로 받는다.


---

# 노출 프론티어 (2026-08-28, 3차)

## 설계 — 노출은 학습 대상이 아니다

1차에서 ①만 미충족이었고 격자에 노출 축이 없었다. 노출을 **후보 격자에 넣지 않았다** —
노출은 학습으로 찾을 전략 파라미터가 아니라 **운용자의 위험 선호**다. 폴드마다 고르면
위험 선호를 과적합하게 되고, 게다가 MAR 은 노출에 거의 불변이라 그 선택이 난수가 된다.

전역 배수 `--exposure` 로 두고 같은 절차를 네 노출에서 돌렸다. 어느 점을 택할지는 사람이 정한다.

## 측정 전에 남긴 예측 (커밋 `81fa0bf`)

> MDD 는 노출에 거의 비례하므로 노출 ~0.65 에서 ① 이 충족될 가능성이 높다.
> 그렇게 통과한다면 그것은 **"전략이 좋아졌다"가 아니라 "크기를 줄였다"** 는 뜻이다.
> ① 은 이 축을 열면 산술적으로 거의 자동 충족되므로, 판단의 무게는 ②(MAR) 에 두어야 한다.

## 결과

| 노출 | 누적 | CAGR | MDD | MAR | ① 위험절반 | ② MAR | ③ 연도 | 판정 |
|---|---|---|---|---|---|---|---|---|
| 100% | +363.9% | 22.9% | 34.1% | 0.67 | X | O | 5/8 | 미충족 |
| 75% | +235.4% | 17.7% | 25.3% | 0.70 | X | O | 5/8 | 미충족 |
| **65%** | **+190.4%** | **15.4%** | **22.3%** | **0.69** | **O** | **O** | **5/8** | **충족** |
| 50% | +144.6% | 12.8% | 17.4% | 0.74 | O | O | 6/8 | 충족 |
| *벤치마크* | *+475.0%* | *26.5%* | *45.4%* | *0.58* | | | | |

기준(MDD ≤ 22.7%)은 **노출 65% 이하에서 충족**된다. 예측이 맞았다.

## 읽는 법

**MAR 이 노출에 거의 불변이다**(0.67 → 0.70 → 0.69 → 0.74). 예측대로 수익과 MDD 가 함께
비례 축소되기 때문이다. 이것이 뜻하는 바는 분명하다 — **①은 크기 조절로 달성된 것이지
전략이 개선된 것이 아니다.** 이 해석은 측정 전에 커밋해 두었다.

**의미 있는 결과는 ②다.** MAR 0.67~0.74 vs 벤치마크 0.58 — 모든 노출에서 일관되게 우위다.
사전 선언 기준, 미관측 구간, 기계적 선택으로 얻은 결과이므로 이 프로젝트에서 처음으로
신뢰할 만한 긍정 신호다.

**그러나 선언한 기준은 충족됐다.** 결과를 본 뒤 "①은 사실 의미 없다"며 기준을 올리는 것은
반대 방향의 골대 옮기기다. 기준은 사용자가 정했고, 미리 커밋했고, 충족됐다.

## 라이브 적용까지 남은 것

조건 충족 = 즉시 재개가 아니다. 선언한 다음 단계는 **"모의운용 후 단계적 재개 검토"** 다.

1. **코드 변경** — 현재 라이브는 ATR 트레일을 쓴다. 워크포워드가 고른 것은 트레일 없는
   MA 청산 계열이다. `TREND_EXIT_*` 구조를 바꿔야 하고, 청산 사다리 동치 테스트
   (`test_exit_ladder_parity.py`)도 함께 고쳐야 한다.
2. **연간 재선택 규칙** — 폴드마다 다른 규칙이 뽑혔다(4종). MAR 0.69 는 특정 규칙의 성적이
   아니라 **"매년 학습 4년으로 다시 고르는 절차"** 의 성적이다. 그 절차를 운영 규칙으로
   문서화하지 않으면 재현되지 않는다.
3. **모의운용** — 백테스트는 항상 모델가 체결을 가정한다. 실제 슬리피지·부분체결·거래정지는
   반영되지 않는다.
4. **노출 결정** — 65% 와 50% 중 어디를 택할지는 위험 선호 문제다(수익 +190% vs +145%,
   MDD 22.3% vs 17.4%).

그때까지 `TREND_ENTRY_HALT=true` 유지.
